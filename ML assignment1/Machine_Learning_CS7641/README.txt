The environment: python 3.6, scikit-learn 0.18.1
Used libraries: numpy, scipy, scikit-learn, pandas, gzip

The main project is in "data_validation.py", "input.py" is only for reading data
All you need is to run "data_validation", it could output all the results of dataset1 and dataset2
The input data are with the codes